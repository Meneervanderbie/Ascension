using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Entity : MonoBehaviour {

    // A Class that all entities inherit from. 
    // Needs Enum value to denote all possible behaviours
    // Possible interactions are planned via GOAP

    public int speed;
    public Tile tile;
}
